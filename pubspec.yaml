name: flutter_qjs_next
description: QuickJS bindings for Flutter and Dart through dart:ffi (flutter_qjs_next), with native support for Android, iOS, macOS, Linux, and Windows.
version: 1.2.0
homepage: https://github.com/NanCunChild/flutter_qjs_next
repository: https://github.com/NanCunChild/flutter_qjs_next
issue_tracker: https://github.com/NanCunChild/flutter_qjs_next/issues
topics:
  - quickjs
  - javascript
  - flutter-qjs-next
  - ffi
  - flutter-plugin

environment:
  sdk: ^3.10.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.1.1

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: FlutterQjsPlugin
      linux:
        pluginClass: FlutterQjsPlugin
      android:
        pluginClass: FlutterQjsPlugin
        package: io.github.nancunchild.flutter_qjs_next
      macos:
        pluginClass: FlutterQjsPlugin
      ios:
        pluginClass: FlutterQjsPlugin
  assets:
    - assets/js/
